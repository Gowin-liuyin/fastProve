# Protocol §3.1 — F1: FP32 structural-only
# Full obfuscation graph retained; only e_0, C_ℓ, ξ_ℓ zeroed (protocol §2).
condition_id: F1
precision: fp32
mode: structural
description: >
  FP32 structural-only: full obfuscation graph (augmented D+R, mixed weights,
  Q/K transform, value mixing, KV cache format, FFN permutation/scaling),
  noise injection terms zeroed.
stress: false
